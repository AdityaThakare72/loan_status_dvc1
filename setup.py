from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='model for predicting if loan will be approved or not',
    author='aditya',
    license='',
)
